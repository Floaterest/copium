name: Count Files

on:
  workflow_dispatch:
  push:
    paths: ['*/']

jobs:
  count:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Adding markdown
        run: echo "# $(find $(ls -d */) -type f | wc -l) Files" >> $GITHUB_STEP_SUMMARY
